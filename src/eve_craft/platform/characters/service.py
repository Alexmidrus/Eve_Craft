from __future__ import annotations


class CharacterService:
    def describe_management(self) -> str:
        return (
            "Character management belongs to the service layer. "
            "The UI action is connected, but character selection and ESI-backed profiles are not implemented yet."
        )
